/**
 * Shared types for @munin-media/ui-native-tamagui.
 */

export interface TamaguiComponentProps {
  /** Test ID for testing */
  testID?: string;
}

/** Token color value — a Tamagui token reference (e.g. '$primary') or raw color */
export type TokenColor = string;
